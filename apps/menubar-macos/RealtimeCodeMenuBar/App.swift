import SwiftUI

@main
struct RealtimeCodeMenuBarApp: App {
    var body: some Scene {
        MenuBarExtra("RealtimeCode", systemImage: "mic") {
            Text("RealtimeCode MVP scaffold")
        }
    }
}
